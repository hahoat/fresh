{
  "name": "fresh-restaurant-manager",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "node server.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && node server.mjs",
    "cloudflare:dev": "npm run build && npx wrangler@latest dev",
    "cloudflare:deploy": "npx wrangler@latest deploy",
    "cloudflare:d1:local": "npx wrangler@latest d1 migrations apply fresh-db --local",
    "cloudflare:d1:remote": "npx wrangler@latest d1 migrations apply fresh-db --remote"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
